7.反转整数

#include<iostream>
using namespace std;
//非主流方案(效率高一点)
int reverse(int number)
{
	int sum = 0;
	while (number != 0)
	{
		int num = sum * 10;
		if (num / 10 != sum)
		{//在这儿判断是否溢出
			return 0;
		}
		sum = num;
		sum += number % 10;
		number = number / 10;
	}
	return sum;
}
